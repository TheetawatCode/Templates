# Prompt Library (Cursor Pro)

## Setup (ใช้ครั้งเดียว)
Create Next.js + TS + Tailwind base. Use existing structure. Add Navbar, Hero, Pricing, Footer. Production-ready, a11y, responsive. Keep tokens low; skip long explanations.

## Feature (ย่อย)
Add [feature-name] in `src/components/sections/[feature].tsx`. Tailwind only, TS typed, ≤120 lines, no extra deps.

## Refactor
Refactor this file for readability + SRP. Extract UI into `src/components/ui`. Keep API the same. Add JSDoc to exports.

## SEO
Implement `og({ title, desc })` and wire to main page (see `src/lib/seo.ts`).

## Bugfix
Fix the bug causing [symptom]. Change the minimal lines and show patch diff.
